

name: actions_on_main_branch
on: 
  pull_request:
    types:
      - opened
    branches:    
      - 'main'
jobs:
  check-user-admin:
    name: job1
    runs-on: ubuntu-latest
    env:
      NAME_QTTQ23: qttq23
    steps:
      - name: st1
        if: ${{ github.actor != env.NAME_QTTQ23 }}
        run: exit 1
      - name: st2
        if: ${{ github.actor == env.NAME_QTTQ23 }}
        run: echo "ok"
      
  
      
  
